/*Escribe una función que sume dos numeros o dos strings*/
function sumOfTwo() {}

/*Escribe una función que sume todos los numeros que se le 
proporcionen independientemente del numero*/
function sumOfAll() {}

module.exports = {
  sumOfTwo,
  sumOfAll,
};
